---
/** i18n Redirection Page */


type Octa = {
  brain: number
}

const octavian: Octa = {
  brain: 0
}

if (octavian.brain === 0) {
  console.log("doo doo brain")
}

type User = {
  name: string,
  age?: number | string | Date
}

const user: User = {
  name: 'John',
  age: 12
}

function printAge(u: User): void {
  if (typeof u.age === 'number') {
    console.log(u.age.toFixed(2))
  } else if (typeof u.age === 'string') {
    console.log(u.age.length)
  } else {
    console.log(u.age?.getFullYear())
  }
}

printAge(user)

console.log("Server")
---


<script>
  console.log("Client")
</script>
